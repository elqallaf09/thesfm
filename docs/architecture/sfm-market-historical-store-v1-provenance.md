# Observation provenance

Each stored observation keeps the upstream provider name and symbol, source class, observation and receipt timestamps, delay/cache metadata and SFM quality state. THE SFM is the normalization/validation layer; provenance identifies where the underlying fact came from.
