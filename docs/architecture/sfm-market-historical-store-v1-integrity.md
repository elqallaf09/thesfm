# Evidence integrity

The evidence hash is content-based and includes the source, symbol, observation/receipt timestamps and quote fields. Identical observations deduplicate; a changed price, source or timestamp creates a distinct immutable observation.
