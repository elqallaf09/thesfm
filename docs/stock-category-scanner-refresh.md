# Refresh cadence

The scanner panel refreshes automatically every five minutes and also exposes a manual refresh action. Normal requests use bounded server caching; `refresh=1` bypasses response caching for a fresh upstream scan when available.
