'use client';

// Keep this historical import path as a compatibility facade. The active
// implementation and its semantic styling live in one canonical component.
export { WisdomTicker } from '../../WisdomTicker';
