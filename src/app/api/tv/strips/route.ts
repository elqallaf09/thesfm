// The strip catalog uses the same exchange identities and coverage counts as
// the TV browser. Prices are paged through /api/tv/snapshot for visible rows.
export { GET, dynamic, maxDuration } from '../catalog/route';
