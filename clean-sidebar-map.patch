diff --git a/src/trader-app/public/cinema.css b/src/trader-app/public/cinema.css
index 1c3f8af..651f806 100644
--- a/src/trader-app/public/cinema.css
+++ b/src/trader-app/public/cinema.css
@@ -130,16 +130,20 @@ button {
 }
 
 .brand-card {
-  display: grid;
-  grid-template-columns: 48px 1fr;
-  gap: 12px;
+  display: flex;
+  gap: 11px;
   align-items: center;
-  min-height: 72px;
-  padding: 12px;
-  border: 1px solid rgba(255, 209, 102, 0.2);
-  border-radius: 22px;
-  background: linear-gradient(145deg, rgba(255, 209, 102, 0.11), rgba(85, 215, 255, 0.06));
-  box-shadow: inset 0 1px 0 rgba(255,255,255,0.08), 0 20px 48px rgba(0,0,0,0.22);
+  min-height: 52px;
+  padding: 8px 6px;
+  border: none;
+  border-radius: 12px;
+  background: transparent;
+  box-shadow: none;
+  transition: background 160ms ease;
+}
+
+.brand-card:hover {
+  background: rgba(34, 211, 238, 0.05);
 }
 
 .brand-mark {
@@ -147,21 +151,23 @@ button {
   place-items: center;
   color: #061016;
   background: linear-gradient(135deg, var(--gold), #45f0a2 52%, #55d7ff);
-  box-shadow: none;
+  box-shadow: 0 0 14px rgba(69, 240, 162, 0.25);
 }
 
 .brand-mark {
-  width: 46px;
-  height: 46px;
-  border-radius: 15px;
+  width: 36px;
+  height: 36px;
+  border-radius: 11px;
   font-family: var(--font-mono);
   font-weight: 800;
 }
 
 .brand-card strong {
   display: block;
-  color: var(--gold);
-  font-size: 1.05rem;
+  color: var(--text);
+  font-size: 0.98rem;
+  font-weight: 700;
+  letter-spacing: 0.01em;
   line-height: 1.1;
 }
 
@@ -181,7 +187,7 @@ button {
   flex-direction: column;
   gap: 2px;
   margin-top: 14px;
-  max-height: calc(100vh - 210px);
+  max-height: calc(100vh - 118px);
   padding-left: 4px;
   overflow: auto;
   scrollbar-width: thin;
@@ -1147,58 +1153,171 @@ tr:last-child td {
 }
 .world-map {
   position: relative;
-  min-height: 360px;
+  min-height: 420px;
   border: 1px solid var(--line);
   border-radius: var(--radius-md);
-  background-color: rgba(3, 10, 18, 0.6);
+  background-color: rgba(2, 8, 15, 0.72);
   background-image:
-    radial-gradient(ellipse 60% 50% at 30% 30%, rgba(85, 215, 255, 0.07), transparent 60%),
-    radial-gradient(ellipse 50% 50% at 75% 65%, rgba(69, 240, 162, 0.05), transparent 60%),
-    url("/assets/world-dotted-map.png");
-  background-size: auto, auto, cover;
-  background-position: center;
-  background-repeat: no-repeat;
+    radial-gradient(ellipse 70% 60% at 50% 38%, rgba(85, 215, 255, 0.05), transparent 65%),
+    linear-gradient(180deg, rgba(4, 14, 24, 0.35), rgba(2, 6, 12, 0.75));
   overflow: hidden;
 }
+
+/* شبكة خطوط طول/عرض خافتة تعطي واقعية خرائط الطيران */
+.world-map::before {
+  position: absolute;
+  inset: 0;
+  z-index: 1;
+  content: "";
+  pointer-events: none;
+  background-image:
+    repeating-linear-gradient(90deg, rgba(85, 215, 255, 0.045) 0 1px, transparent 1px 90px),
+    repeating-linear-gradient(180deg, rgba(85, 215, 255, 0.04) 0 1px, transparent 1px 70px);
+  mask-image: radial-gradient(ellipse 90% 80% at 50% 50%, black 40%, transparent 95%);
+}
+
+/* مسح رادار ضوئي يعبر الخريطة ببطء */
+.world-map::after {
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  z-index: 2;
+  width: 90px;
+  content: "";
+  pointer-events: none;
+  background: linear-gradient(90deg, transparent, rgba(85, 215, 255, 0.07) 45%, rgba(69, 240, 162, 0.05) 55%, transparent);
+  animation: map-sweep 14s linear infinite;
+}
+
+@keyframes map-sweep {
+  from { left: -12%; }
+  to { left: 112%; }
+}
+
+.world-map-img {
+  position: absolute;
+  inset: 0;
+  z-index: 0;
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  opacity: 1;
+  filter: brightness(1.35) contrast(1.18) saturate(1.25) drop-shadow(0 0 18px rgba(34, 211, 238, 0.12));
+  pointer-events: none;
+}
+
 .world-map svg {
   position: absolute;
   inset: 0;
+  z-index: 2;
   width: 100%;
   height: 100%;
 }
+
 .world-map svg path {
   fill: none;
-  stroke: rgba(85, 215, 255, 0.25);
-  stroke-width: 1.2;
-  stroke-dasharray: 4 6;
-  animation: mapdash 24s linear infinite;
+  stroke: rgba(85, 215, 255, 0.3);
+  stroke-width: 1.1;
+  stroke-dasharray: 3 8;
+  stroke-linecap: round;
+  filter: drop-shadow(0 0 3px rgba(85, 215, 255, 0.35));
+  animation: mapdash 20s linear infinite;
 }
-@keyframes mapdash { to { stroke-dashoffset: -200; } }
+
+@keyframes mapdash { to { stroke-dashoffset: -220; } }
+
+/* عقد الجلسات: شرائح مقروءة فوق الخريطة */
 .map-node {
   position: absolute;
+  z-index: 3;
   display: flex;
+  gap: 7px;
   align-items: center;
-  gap: 6px;
-  font: 600 0.66rem var(--font-ar);
+  padding: 4px 9px 4px 7px;
+  border: 0.5px solid rgba(143, 180, 205, 0.22);
+  border-radius: 999px;
   color: var(--muted);
+  background: rgba(3, 11, 19, 0.82);
+  font: 600 0.64rem var(--font-ar);
   white-space: nowrap;
+  transform: translate(-4px, -50%);
+  transition: border-color 160ms ease, background 160ms ease;
+}
+
+.map-node:hover {
+  border-color: rgba(85, 215, 255, 0.45);
+  background: rgba(5, 16, 27, 0.94);
 }
+
+.map-node b {
+  font-weight: 700;
+  letter-spacing: 0.02em;
+}
+
+.map-node small {
+  color: var(--muted-2);
+  font-size: 0.58rem;
+}
+
 .map-node i {
-  width: 8px; height: 8px;
+  position: relative;
+  flex-shrink: 0;
+  width: 8px;
+  height: 8px;
+  border-radius: 50%;
+}
+
+/* الجلسة المفتوحة: أخضر نابض مع حلقة رادار متمددة */
+.map-node.is-open {
+  border-color: rgba(69, 240, 162, 0.3);
+}
+
+.map-node.is-open i {
+  background: #45f0a2;
+  box-shadow: 0 0 10px rgba(69, 240, 162, 0.85);
+}
+
+.map-node.is-open i::after {
+  position: absolute;
+  inset: -3px;
+  content: "";
+  border: 1px solid rgba(69, 240, 162, 0.7);
   border-radius: 50%;
-  background: var(--cyan);
-  box-shadow: 0 0 0 4px rgba(85, 215, 255, 0.14), 0 0 12px var(--cyan);
-  animation: nodepulse 3s ease-in-out infinite;
-}
-@keyframes nodepulse { 50% { box-shadow: 0 0 0 7px rgba(85, 215, 255, 0.06), 0 0 16px var(--cyan); } }
-.map-node.node-0 { top: 38%; left: 12%; }
-.map-node.node-1 { top: 28%; left: 46%; }
-.map-node.node-2 { top: 36%; left: 54%; }
-.map-node.node-3 { top: 56%; left: 60%; }
-.map-node.node-4 { top: 62%; left: 52%; }
-.map-node.node-5 { top: 48%; left: 64%; }
-.map-node.node-6 { top: 44%; left: 84%; }
-.map-node.node-7 { top: 58%; left: 80%; }
+  animation: node-ring 2.4s ease-out infinite;
+}
+
+.map-node.is-open b {
+  color: #cdf7e3;
+}
+
+.map-node.is-open small {
+  color: rgba(69, 240, 162, 0.8);
+}
+
+@keyframes node-ring {
+  0% { transform: scale(0.6); opacity: 1; }
+  100% { transform: scale(2.6); opacity: 0; }
+}
+
+/* الجلسة المغلقة: خافتة ساكنة */
+.map-node.is-closed {
+  opacity: 0.72;
+}
+
+.map-node.is-closed i {
+  background: #ff6b74;
+  box-shadow: 0 0 6px rgba(255, 107, 116, 0.45);
+}
+
+.map-node.is-closed b {
+  color: var(--muted);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .world-map::after { animation: none; display: none; }
+  .world-map svg path { animation: none; }
+  .map-node.is-open i::after { animation: none; display: none; }
+}
 .mo-gauges { display: grid; gap: 12px; align-content: start; }
 .mo-gauge {
   border: 1px solid var(--line);
@@ -1621,8 +1740,9 @@ body,
 .page-stack { display: grid; gap: 16px; }
 .panel { padding: clamp(16px, 1.8vw, 20px); }
 
-/* keep RTL sidebar nav highlight subtle (no horizontal shift overflow) */
-.sidebar-nav a:hover, .sidebar-nav a.is-active { transform: none; }
+/* keep RTL sidebar nav highlight subtle (active stays anchored, hover slides inward) */
+.sidebar-nav a.is-active { transform: none; }
+.sidebar-nav a:hover:not(.is-active) { transform: translateX(-3px); }
 
 /* brand/profile cards must never get the nav active treatment */
 .brand-card.is-active, .profile-card.is-active, .ai-pro-card.is-active { transform: none; }
@@ -2687,7 +2807,7 @@ body::after {
 }
 
 .brand-card {
-  border-radius: 18px;
+  border-radius: 12px;
 }
 
 .brand-mark,
@@ -2712,9 +2832,9 @@ body::after {
 }
 
 .sidebar-nav a {
-  min-height: 46px;
+  min-height: 42px;
   border-color: transparent;
-  border-radius: 13px;
+  border-radius: 11px;
   color: var(--terminal-text-secondary);
   background: transparent;
   box-shadow: none;
@@ -2722,14 +2842,17 @@ body::after {
 
 .sidebar-nav a:hover,
 .sidebar-nav a.is-active {
-  border-color: var(--terminal-border-strong);
+  border-color: transparent;
   color: var(--terminal-text);
-  background: linear-gradient(90deg, rgba(34, 211, 238, 0.14), rgba(21, 49, 79, 0.72));
-  transform: none;
+  box-shadow: none;
+}
+
+.sidebar-nav a:hover {
+  background: rgba(34, 211, 238, 0.05);
 }
 
 .sidebar-nav a.is-active {
-  box-shadow: inset 3px 0 0 var(--terminal-accent);
+  background: linear-gradient(270deg, rgba(34, 211, 238, 0.12), rgba(34, 211, 238, 0.02) 70%, transparent);
 }
 
 .collapse-btn,
@@ -2994,26 +3117,16 @@ select:focus-visible {
 
 .world-map {
   border-color: var(--terminal-border);
-  background:
-    linear-gradient(180deg, rgba(10, 23, 40, 0.92), rgba(6, 17, 31, 0.92)),
-    linear-gradient(90deg, rgba(34, 211, 238, 0.06), rgba(20, 184, 166, 0.05));
 }
 
 .world-map svg path {
-  stroke: rgba(34, 211, 238, 0.34);
-  animation: none;
+  stroke: rgba(34, 211, 238, 0.32);
 }
 
 .map-node {
   color: var(--terminal-text-secondary);
 }
 
-.map-node i {
-  background: var(--terminal-accent);
-  box-shadow: 0 0 0 4px rgba(34, 211, 238, 0.12);
-  animation: none;
-}
-
 .mo-gauge,
 .asset-metrics span,
 .detail-card,
@@ -3558,7 +3671,6 @@ body::after {
 .sidebar-status,
 .profile-card,
 .ai-pro-card,
-.brand-card,
 .loading-panel,
 .empty-state,
 .page-hero,
@@ -3638,13 +3750,8 @@ body::after {
 
 .brand-card {
   transform-style: preserve-3d;
-  background:
-    linear-gradient(145deg, rgba(255, 212, 107, 0.18), rgba(65, 217, 255, 0.1)),
-    linear-gradient(180deg, rgba(255, 255, 255, 0.08), transparent);
-  box-shadow:
-    inset 0 1px 0 rgba(255, 255, 255, 0.16),
-    0 6px 0 rgba(66, 47, 9, 0.78),
-    0 28px 58px rgba(0, 0, 0, 0.38);
+  background: transparent;
+  box-shadow: none;
 }
 
 .brand-mark,
@@ -4277,14 +4384,7 @@ a.badge:focus-visible,
 .rec-market-chips .chip { padding: 5px 12px; border-radius: 999px; border: 1px solid var(--line); background: rgba(15,34,56,.55); color: var(--muted); font-size: 12px; cursor: pointer; }
 .rec-market-chips .chip.is-active { border-color: rgba(85,215,255,.5); color: #7fe3ff; background: rgba(30,80,120,.35); }
 
-.world-map-img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; opacity: 0.9; pointer-events: none; }
-
-.world-map-img { z-index: 0; }
-.map-node { z-index: 2; }
-.map-node small { display: block; font-size: 9px; color: var(--muted); margin-top: 2px; white-space: nowrap; }
-.map-node.is-open i { background: #45f0a2; box-shadow: 0 0 10px rgba(69,240,162,.8); }
-.map-node.is-closed i { background: #ff6b74; box-shadow: 0 0 10px rgba(255,107,116,.7); }
-.map-node.is-open b { color: #bff5dd; }
+/* أنماط الخريطة الموحدة معرفة في كتلة .world-map الرئيسية أعلاه */
 
 /* ═══════════════════════════════════════════════════════════════
    MARKET CINEMA — الخلفية السينمائية (شموع شبحية + خط سعر + جزيئات)
@@ -4310,7 +4410,7 @@ a.badge:focus-visible,
   position: absolute;
   width: min(34vw, 480px);
   height: min(52vh, 620px);
-  opacity: 0.055;
+  opacity: 0.085;
 }
 
 .mc-candles .mc-wick { stroke: #9fd8ef; stroke-width: 2; }
@@ -4439,7 +4539,7 @@ a.badge:focus-visible,
 
 /* الجوال: طبقة أخف — شموع وجزيئات أقل حفاظاً على البطارية */
 @media (max-width: 720px) {
-  .mc-candles { width: 52vw; opacity: 0.04; }
+  .mc-candles { width: 52vw; opacity: 0.06; }
   .mc-p-up:nth-of-type(4), .mc-p-down:nth-of-type(7) { display: none; }
   .mc-priceline { opacity: 0.35; }
 }
@@ -4454,3 +4554,105 @@ a.badge:focus-visible,
   .mc-priceline .mc-line-dot { display: none; }
   .mc-p { display: none; }
 }
+
+/* ═══════════════════════════════════════════════════════════════
+   VISUAL POLISH — تنظيف سطر التشخيص، حالات عدم التوفر، هياكل نابضة
+   ═══════════════════════════════════════════════════════════════ */
+
+/* صف تشخيص المزود: معلومة مطوّر — سطر mono واحد خافت،
+   نخفي الضجيج (fallback + أعلام المزودين + رمز المزود) ونبقي الاسم والوقت */
+.leadership-provider-row {
+  display: flex;
+  gap: 8px;
+  align-items: center;
+  justify-content: space-between;
+  margin-top: 8px;
+  padding-top: 8px;
+  border-top: 1px solid rgba(143, 167, 186, 0.1);
+  color: var(--muted-2);
+  font-family: var(--font-mono);
+  font-size: 0.58rem;
+  letter-spacing: 0.03em;
+  opacity: 0.75;
+}
+
+.leadership-provider-row > span {
+  display: none;
+}
+
+.leadership-provider-row b {
+  overflow: hidden;
+  font-weight: 600;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+.leadership-provider-row time {
+  flex-shrink: 0;
+}
+
+/* بطاقات القيادة غير المتاحة: باهتة ومنكمشة بصرياً بدل صناديق فارغة صارخة */
+.leadership-card:has(.signal-badge.unavailable) {
+  border-color: rgba(143, 167, 186, 0.1);
+  background: rgba(8, 20, 32, 0.4);
+  opacity: 0.55;
+  filter: saturate(0.5);
+  transition: opacity 180ms ease, filter 180ms ease;
+}
+
+.leadership-card:has(.signal-badge.unavailable):hover {
+  opacity: 0.85;
+  filter: saturate(0.85);
+}
+
+.leadership-card:has(.signal-badge.unavailable) .quality-badge {
+  opacity: 0.6;
+}
+
+/* خلايا الخريطة الحرارية غير المتاحة: هيكل نابض هادئ بدل نص مكرر صارخ */
+@keyframes mc-skeleton-pulse {
+  0%, 100% { background-color: rgba(8, 20, 32, 0.5); border-color: rgba(143, 167, 186, 0.1); }
+  50% { background-color: rgba(15, 34, 56, 0.55); border-color: rgba(143, 167, 186, 0.2); }
+}
+
+.opportunity-cell.unavailable,
+.heat-cell.unavailable {
+  animation: mc-skeleton-pulse 3.2s ease-in-out infinite;
+  opacity: 0.6;
+}
+
+.opportunity-cell.unavailable span,
+.opportunity-cell.unavailable em,
+.heat-cell.unavailable small,
+.heat-cell.unavailable em {
+  color: var(--muted-2);
+  opacity: 0.55;
+}
+
+.opportunity-cell.unavailable .asset-logo,
+.heat-cell.unavailable .asset-logo {
+  filter: grayscale(0.6);
+  opacity: 0.7;
+}
+
+.opportunity-cell.unavailable:hover,
+.heat-cell.unavailable:hover {
+  animation-play-state: paused;
+  opacity: 0.9;
+}
+
+/* تباعد أوسع بين مجموعات القائمة الجانبية */
+.nav-group-label {
+  margin-top: 18px;
+}
+
+.nav-group-label:first-child {
+  margin-top: 4px;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .opportunity-cell.unavailable,
+  .heat-cell.unavailable {
+    animation: none;
+  }
+}
diff --git a/src/trader-app/public/index.html b/src/trader-app/public/index.html
index c2659d4..4572d78 100644
--- a/src/trader-app/public/index.html
+++ b/src/trader-app/public/index.html
@@ -8,7 +8,7 @@
     <link rel="preconnect" href="https://fonts.googleapis.com" />
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
     <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@400;500;600;700;800&family=JetBrains+Mono:wght@500;700&display=swap" rel="stylesheet" />
-    <link rel="stylesheet" href="/cinema.css?v=20260705-cinematic-nav" />
+    <link rel="stylesheet" href="/cinema.css?v=20260705-clean-map" />
   </head>
   <body>
     <div class="market-cinema" aria-hidden="true">
@@ -62,9 +62,8 @@
         <div class="sidebar-brand-row">
           <a class="brand-card" href="/thesfm-trader-own" data-route-link data-route="dashboard">
             <span class="brand-mark" aria-hidden="true">S</span>
-            <span>
+            <span class="brand-name">
               <strong>the-sfm trader</strong>
-              <small>AI market command center</small>
             </span>
           </a>
           <button id="sidebar-collapse" class="collapse-btn" type="button" aria-label="طي القائمة" title="طي القائمة">«</button>
@@ -91,27 +90,6 @@
           <a href="/thesfm-trader-own/settings" data-route-link data-route="settings"><svg class="nav-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="3.2"/><path d="M19 12a7 7 0 0 0-.1-1.2l2-1.5-2-3.4-2.3 1a7 7 0 0 0-2-1.2L14.2 3h-4l-.4 2.7a7 7 0 0 0-2 1.2l-2.3-1-2 3.4 2 1.5A7 7 0 0 0 5 12c0 .4 0 .8.1 1.2l-2 1.5 2 3.4 2.3-1a7 7 0 0 0 2 1.2l.4 2.7h4l.4-2.7a7 7 0 0 0 2-1.2l2.3 1 2-3.4-2-1.5c.1-.4.1-.8.1-1.2Z"/></svg><span class="nav-label">الإعدادات</span><small>Settings</small></a>
         </nav>
 
-        <a class="ai-pro-card" href="/thesfm-trader-own/ai-scanner" data-route-link>
-          <span class="ai-pro-badge">AI PRO</span>
-          <strong>محرك التحليل الذكي</strong>
-          <small>فعّل الماسح للحصول على أقوى الإشارات اللحظية.</small>
-        </a>
-
-        <section class="sidebar-status" aria-live="polite">
-          <span class="status-dot" id="sidebar-status-dot"></span>
-          <div>
-            <strong id="sidebar-status-title">فحص المزود</strong>
-            <small id="sidebar-status-copy">جاري تحميل حالة البيانات</small>
-          </div>
-        </section>
-
-        <a class="profile-card" href="/thesfm-trader-own/settings" data-route-link data-route="settings">
-          <span class="profile-avatar" aria-hidden="true">M</span>
-          <span>
-            <strong>SFM Trader</strong>
-            <small>عضو · الإعدادات</small>
-          </span>
-        </a>
       </aside>
 
       <div class="terminal-stage">
